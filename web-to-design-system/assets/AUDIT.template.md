# 设计系统审计记录 · {{NAME}}

> 由 web-to-design-system 生成。记录这套系统从哪里来、哪些是实测、哪些是推断、哪些还缺；`DESIGN.md` 只写意图，`tokens/` 只写值，证据与决定都在这里。旧规范不会被此流程覆盖或删除。

## 本次决策

- 来源页面：{{SOURCES}}
- 取证方式：agent-browser 在真实渲染页面里读取计算样式、样式表规则、根变量与交互状态（桌面 {{VIEWPORT}}；另加窄屏 / 手机视口的排版与布局探针）；不以截图为主证据。
- 取证时间：{{DATE}}
- 证据文件：{{EVIDENCE}}（系统临时目录，不随项目提交；需要复核时重跑 `extract-evidence.mjs`）
- 确认的权威来源：本目录 `tokens/*.tokens.json`（接入后旧站点的 CSS 只作参考，不双写）
- 品牌族判定：{{BRAND}}
- 首屏模式：{{INITIAL_SCHEME}}；{{THEME_LINE}}

## 角色覆盖

- 观察（有实测证据）：{{OBSERVED}} 个
- 推断（按 `references/mapping-rules.md` 的默认规则补的）：{{INFERRED}} 个——`$description` 以 `[推断]` 开头，接入前逐条确认，确认后改为 `[确认]`
- 缺口（词表里有、本次没填）：{{MISSING}} 个（core 层 {{MISSING_CORE}} 个）

### 推断角色清单

| 角色 | 别名 | 推断依据 | 确认状态 |
| --- | --- | --- | --- |
{{INFERRED_ROWS}}

### 缺口清单

| 角色 | 层 | 含义 | 找证据的位置 | 决定 |
| --- | --- | --- | --- | --- |
{{MISSING_ROWS}}

## 对比度基线

把 `check-contrast.mjs --system <本目录>` 的输出贴在这里；低于底线的配对要么改值、要么由用户拍板登记为例外（写明回补路径），并同步到 `DESIGN.md`「验收基线 · 已批准的例外」。

待填写：对比度报告。

## 未纳管项

- 渐变、复合阴影原式、组件内部几何（对勾位置、滑块位移、圆点直径）不进 Token；接入项目里用 `exemptions.json` 登记并写明理由。
- 待填写：从取证摘要「警告」搬过来的条目（不在 2px 网格上的间距、超过三档的阴影、全大写文字等）与处置。
{{BRIDGE_NOTES}}

## 风险与待确认

- 待填写：需要用户拍板的品牌决定（选中态是否用品牌色、链接是否用色相、暗色是否纳管、状态色的色相归属）。
- 待填写：只看了首页 / 少数页面带来的盲区（表单、表格、弹窗、空状态是否覆盖）。
{{WARNING_ROWS}}

## 后续治理

- 日常文案、数据或图片内容更新不改变设计系统。
- 新增、跨页面复用或修订视觉决定时，先记录提案和用户选择；一次性差异先标为 Drift 或试验。
- 本记录写完后运行 steward：`validate-system` → `build-tokens` → `guard`，三绿再接入。
