policy:
  allow_implicit_invocation: false
interface:
  display_name: "Design System Steward"
  short_description: "Govern DTCG Core Tokens, bounded scopes, and confirmed themes"
  default_prompt: "Use $design-system-steward audit for the explicitly named project root; identify only evidence-backed design-system candidates."
